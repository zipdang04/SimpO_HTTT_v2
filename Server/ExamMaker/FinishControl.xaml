<UserControl x:Class="Server.ExamMaker.FinishControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Server.ExamMaker"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl x:Name="tabControl">
            <TabItem Header="10 điểm">
                <ScrollViewer>
                    <StackPanel x:Name="st1"/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="20 điểm">
                <ScrollViewer>
                    <StackPanel x:Name="st2"/>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="30 điểm">
                <ScrollViewer>
                    <StackPanel x:Name="st3"/>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
